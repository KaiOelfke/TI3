Bei UDP werden gleich große Datenpakete verschickt, waehrend bei TCP erstmal nur drei kleine Header geschickt werden. Es handelt sich dabei vermutlich um den 3 Way Handshake. Ausserdem gibt es bei TCP viel mehr Optionen und Konfigurationen im Header. Bei UDP werden fast nur die Daten geschickt und bei TCP wird viel mehr uberprueft. Der Grund dafuer ist, dass es bei UDP der Client einfach die Daten schickt. Es gibt keine Ueberpruefung ob der Server die Daten bekommt. UDP ist also effizienter bei der Datenuebertragung, aber nicht zwangslaeufig bei der Informationsuebertragung, weil natuerlich Probleme auftreten koennen. TCP hat mehr Overhead, aber ist sicherer und zuverlaessiger.